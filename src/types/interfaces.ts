export interface IconDefaultProps {
  title: string;
  ariaHidden?: boolean;
  ariaLabel?: string;
  className?: string;
  height?: string;
  size?: string;
}

export interface TextInfoList {
  description: string;
  strongText: string;
}
